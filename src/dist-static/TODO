Better pool stats API.

Allow user-defined Connection-test queries.

JBoss integration.

Create a dbms-dir level build.xml file, rather than having top-level
manage subdirectories.

	things to complete:
	   preferredTestQuery
	   testConnectionsOnCheckin	







